join_market = {
	requires_approval = yes
	can_use_obligations = yes
	uses_random_approval = yes
	groups = {
		general
	}

	texture = "gfx/interface/icons/diplomatic_action_icons/request_market_control.dds"
	confirmation_sound = "event:/SFX/UI/MapInteraction/SubjectInteractions/diplomatic_action_subjects_12_request_market_control_benign"

	selectable = {
		NOT = { is_country_type = decentralized }
		is_revolutionary = no
		OR = {
			is_subject = no
			is_subject_type = subject_type_market_union
		}
	}

	potential = {
		# El target debe controlar su propio mercado
		scope:target_country = {
			market.owner = this
		}
		# No estar ya en el mismo mercado
		NOT = { market = scope:target_country.market }
	}

	possible = {
		# No debe ser vasallo de otro país (pero puede ser market_union)
		custom_tooltip = {
			text = "JOIN_MARKET_CANNOT_BE_SUBJECT"
			OR = {
				is_subject = no
				is_subject_type = subject_type_market_union
			}
		}
		
		# El actor (quien pide) debe tener MAYOR prestigio que el target
		custom_tooltip = {
			text = "JOIN_MARKET_MORE_PRESTIGE"
			prestige > scope:target_country.prestige
		}

		# El target debe controlar su mercado
		scope:target_country = {
			market.owner = this
		}
	}

	accept_effect = {
		# Mejorar relaciones
		scope:target_country = { 
			change_relations = { 
				country = root 
				value = 15 
			} 
		}
		
		# Mensaje de éxito
		custom_tooltip = join_market_success_effect
	}

	decline_effect = {
		# Empeorar relaciones
		scope:target_country = { 
			change_relations = { 
				country = root 
				value = -10 
			} 
		}
	}

	pact = {
		cost = 0
		has_junior_participant = yes
		maintenance_paid_by = first_country
		forced_duration = 12

		# El actor se convierte en "junior" del target's market
		subject_type = subject_type_market_union

		# Ambos pueden romper libremente
		actor_can_break = {
			always = yes
		}

		target_can_break = {
			always = yes
		}

		manual_break_effect = {
			change_relations = { country = scope:target_country value = -15 }
		}

		auto_break_effect = {
			change_relations = { country = scope:target_country value = -10 }
		}
	}

	ai = {
		evaluation_chance = {
			value = 0.25
		}

		accept_score = {
			value = 0

			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_BASE"
				value = -100
			}

			# Opinión ideológica
			if = {
				limit = {
					"scope:actor.ai_ideological_opinion(root)" > 0
				}
				add = {
					desc = "DIPLOMATIC_ACCEPTANCE_IDEOLOGICAL_OPINION"
					value = "scope:actor.ai_ideological_opinion(root)"
					multiply = 0.5
					max = 25
				}
			}
			else_if = {
				limit = {
					"scope:actor.ai_ideological_opinion(root)" < 0
				}
				add = {
					desc = "DIPLOMATIC_ACCEPTANCE_IDEOLOGICAL_OPINION_NEGATIVE"
					value = "scope:actor.ai_ideological_opinion(root)"
					multiply = 0.5
					min = -25
				}
			}

			# Tamaño económico del que solicita - países más grandes mejoran más tu mercado
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_ACTOR_GDP"
				value = "scope:actor.gdp"
				divide = 5000000
				max = 50
			}

			# Relaciones entre países
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_RELATIONS"
				if = {
					limit = {
						"relations(scope:actor)" >= relations_threshold:friendly
					}
					value = 50
				}
				else_if = {
					limit = {
						"relations(scope:actor)" >= relations_threshold:amicable
					}
					value = 30
				}
				else_if = {
					limit = {
						"relations(scope:actor)" >= relations_threshold:cordial
					}
					value = 15
				}
				else_if = {
					limit = {
						"relations(scope:actor)" > relations_threshold:poor
					}
					value = -20
				}
				else = {
					value = -40
				}
			}

			# Actitud del target hacia el actor
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_ATTITUDE"
				if = {
					limit = {
						has_attitude = {
							who = scope:actor
							attitude = genial
						}
					}
					value = 50
				}
				else_if = {
					limit = {
						OR = {
							has_attitude = {
								who = scope:actor
								attitude = protective
							}
							has_attitude = {
								who = scope:actor
								attitude = cooperative
							}
							has_attitude = {
								who = scope:actor
								attitude = conciliatory
							}
						}
					}
					value = 25
				}
				else_if = {
					limit = {
						OR = {
							has_attitude = {
								who = scope:actor
								attitude = antagonistic
							}
							has_attitude = {
								who = scope:actor
								attitude = domineering
							}
							has_attitude = {
								who = scope:actor
								attitude = belligerent
							}
						}
					}
					value = -100
				}
				else = {
					value = -50
				}
			}

			# Infamia del actor
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_INFAMY"
				if = {
					limit = {
						"scope:actor.infamy" >= infamy_threshold:infamous
						"scope:actor.infamy" < infamy_threshold:notorious
					}
					value = -15
				}
				if = {
					limit = {
						"scope:actor.infamy" >= infamy_threshold:notorious
						"scope:actor.infamy" < infamy_threshold:pariah
					}
					value = -30
				}
				if = {
					limit = {
						"scope:actor.infamy" >= infamy_threshold:pariah
					}
					value = -50
				}
			}

			# Diferencia de rango - preferimos países de rango similar o menor
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_RANK"
				value = country_rank
				subtract = scope:actor.country_rank
				multiply = 10
				max = 30
				min = -30
			}

			# Dependencia económica del actor hacia nosotros
			add = {
				desc = "DIPLOMATIC_ACCEPTANCE_ACTOR_ECONOMIC_DEPENDENCE"
				value = "scope:actor.economic_dependence(root)"
				multiply = 100
				max = 50
			}

			# Bonificación si somos países adyacentes
			if = {
				limit = {
					is_strategic_adjacent_to = scope:actor
				}
				add = {
					desc = "DIPLOMATIC_ACCEPTANCE_STRATEGIC_ADJACENT"
					value = 20
				}
			}

			# Oportunidades de lobby
			if = {
				limit = {
					scope:actor = {
						has_variable = pro_lobby_proposal_market
						var:pro_lobby_opportunity_target ?= scope:target_country
					}
				}
				add = {
					desc = "LOBBY_OPPORTUNITY_BONUS"
					value = 100
				}
			}

			if = {
				limit = {
					scope:actor = {
						has_variable = anti_lobby_proposal_market
						var:anti_lobby_opportunity_target ?= scope:target_country
					}
				}
				add = {
					desc = "LOBBY_OPPORTUNITY_BONUS"
					value = 50
				}
			}
		}

		propose_score = {
			value = 10
		}

		use_obligation_chance = {
			value = 25
		}

		will_propose = {
			# Solo si necesita mejor acceso al mercado
			capital = { 
				market_access < 0.75
			}
			# Y tiene más prestigio que el target
			prestige > scope:target_country.prestige
			# Buenas relaciones
			"relations(scope:target_country)" >= relations_threshold:cordial
		}

		will_propose_even_if_not_accepted = {
			capital = { 
				market_access < 0.5
			}
			"relations(scope:target_country)" >= relations_threshold:amicable
		}
	}
}
